# Модуль интеграции с узбекскими платежными системами

Модуль для интеграции Frappe Framework с платежными системами Узбекистана: Payme, Click и FreedomPay.

## 📋 Содержание

- [Установка](#установка)
- [Настройка](#настройка)
- [Использование](#использование)
- [API](#api)
- [Примеры](#примеры)

## 🚀 Установка

### Требования

- Frappe Framework версии 15.0 или выше
- Bench установлен и настроен
- Приложение `payments` установлено

### Шаги установки

1. **Добавьте приложение в bench:**

```bash
cd /path/to/your/bench
bench get-app uzbek_payments
```

2. **Установите приложение на сайте:**

```bash
bench --site your-site.localhost install-app uzbek_payments
```

3. **Перезапустите bench:**

```bash
bench restart
```

## ⚙️ Настройка

### Настройка Payme

1. Зарегистрируйтесь на [payme.uz](https://payme.uz)
2. Получите Merchant ID и Merchant Key
3. В Frappe перейдите в **Payme Settings**
4. Введите:
   - **Merchant ID** - ваш идентификатор мерчанта
   - **Merchant Key** - ваш секретный ключ
5. Включите **Enabled** если хотите использовать Payme
6. Сохраните

### Настройка Click

1. Зарегистрируйтесь на [my.click.uz](https://my.click.uz)
2. Получите Merchant ID, Service ID и Secret Key
3. В Frappe перейдите в **Click Settings**
4. Введите:
   - **Merchant ID** - ваш идентификатор мерчанта
   - **Service ID** - идентификатор сервиса
   - **Secret Key** - секретный ключ
5. Включите **Enabled** если хотите использовать Click
6. Сохраните

### Настройка FreedomPay

1. Зарегистрируйтесь на [freedompay.uz](https://freedompay.uz)
2. Получите Merchant ID, Terminal ID и Secret Key
3. В Frappe перейдите в **FreedomPay Settings**
4. Введите:
   - **Merchant ID** - ваш идентификатор мерчанта
   - **Terminal ID** - идентификатор терминала
   - **Secret Key** - секретный ключ
5. Включите **Enabled** если хотите использовать FreedomPay
6. Сохраните

## 💻 Использование

### В Python коде

```python
from payments.utils import get_payment_gateway_controller

# Получить контроллер платежной системы
controller = get_payment_gateway_controller("Payme")  # или "Click" или "FreedomPay"

# Проверить валюту
controller().validate_transaction_currency("UZS")

# Создать URL для оплаты
payment_details = {
    "amount": 100000,  # Сумма в UZS (будет автоматически конвертирована в тийины)
    "title": "Оплата заказа #123",
    "description": "Оплата через Payme",
    "reference_doctype": "Sales Invoice",
    "reference_docname": "SI-00001",
    "payer_email": "customer@example.com",
    "payer_name": "Иван Иванов",
    "order_id": "ORDER-123",
    "currency": "UZS",
    "redirect_to": "/payment-success",
}

payment_url = controller().get_payment_url(**payment_details)
# Перенаправить пользователя на payment_url
```

### В Web Forms

1. Перейдите в настройки **Web Form**
2. Включите **Accept Payment**
3. Выберите **Payme**, **Click** или **FreedomPay** как Payment Gateway
4. Установите сумму и валюту (UZS)

### Обработка статуса оплаты

Когда оплата завершена, система вызовет метод `on_payment_authorized` на документе-ссылке.

Пример:

```python
def on_payment_authorized(self, payment_status):
    """Вызывается когда оплата авторизована"""
    if payment_status == "Completed":
        # Обновить статус документа
        self.status = "Paid"
        self.save()
        
        # Отправить email подтверждения
        self.send_confirmation_email()
        
        return "/payment-success"  # Опционально: кастомный URL для редиректа
```

## 🔌 API

### Callback URLs

Платежные системы отправляют callback на:

- **Payme**: `/api/method/uzbek_payments.payment_gateways.doctype.payme_settings.payme_settings.callback`
- **Click**: `/api/method/uzbek_payments.payment_gateways.doctype.click_settings.click_settings.callback`
- **FreedomPay**: `/api/method/uzbek_payments.payment_gateways.doctype.freedompay_settings.freedompay_settings.callback`

Убедитесь, что эти URL доступны из интернета.

### Методы

#### `get_payment_url(**kwargs)`

Создает URL для оплаты.

**Параметры:**
- `amount` (float) - сумма в UZS
- `title` (str) - заголовок платежа
- `description` (str) - описание платежа
- `reference_doctype` (str) - тип документа-ссылки
- `reference_docname` (str) - имя документа-ссылки
- `payer_email` (str) - email плательщика
- `payer_name` (str) - имя плательщика
- `order_id` (str) - ID заказа
- `currency` (str) - валюта (только "UZS")
- `redirect_to` (str) - URL для редиректа после успешной оплаты

**Возвращает:** URL для оплаты

#### `validate_transaction_currency(currency)`

Проверяет поддерживаемую валюту.

**Параметры:**
- `currency` (str) - валюта для проверки

**Выбрасывает:** ValidationError если валюта не поддерживается

## 💰 Валюта

Все платежные системы (Payme, Click и FreedomPay) поддерживают только валюту **UZS** (Узбекский сум).

Суммы автоматически конвертируются в тийины (1 UZS = 100 тийинов) для платежных систем.

## 📝 Примеры

### Пример 1: Оплата счета

```python
import frappe
from payments.utils import get_payment_gateway_controller

def create_payment_for_invoice(invoice_name):
    """Создать платеж для счета"""
    invoice = frappe.get_doc("Sales Invoice", invoice_name)
    
    controller = get_payment_gateway_controller("Payme")
    
    payment_details = {
        "amount": invoice.grand_total,
        "title": f"Оплата счета {invoice.name}",
        "description": f"Оплата счета {invoice.name} на сумму {invoice.grand_total} UZS",
        "reference_doctype": "Sales Invoice",
        "reference_docname": invoice.name,
        "payer_email": invoice.contact_email or invoice.customer_email,
        "payer_name": invoice.customer_name,
        "order_id": invoice.name,
        "currency": "UZS",
        "redirect_to": f"/invoices/{invoice.name}",
    }
    
    payment_url = controller().get_payment_url(**payment_details)
    return payment_url
```

### Пример 2: Обработка успешной оплаты

```python
def on_payment_authorized(self, payment_status):
    """Обработка успешной оплаты"""
    if payment_status == "Completed":
        # Обновить статус счета
        self.status = "Paid"
        self.db_set("status", "Paid")
        
        # Создать запись о платеже
        payment_entry = frappe.get_doc({
            "doctype": "Payment Entry",
            "payment_type": "Receive",
            "party_type": "Customer",
            "party": self.customer,
            "paid_amount": self.grand_total,
            "received_amount": self.grand_total,
            "reference_no": self.name,
            "reference_date": frappe.utils.today(),
        })
        payment_entry.insert()
        payment_entry.submit()
        
        # Отправить уведомление
        frappe.sendmail(
            recipients=[self.contact_email],
            subject=f"Оплата счета {self.name} получена",
            message=f"Ваш счет {self.name} успешно оплачен.",
        )
        
        return f"/invoices/{self.name}"
```

## 🔒 Безопасность

- Все секретные ключи хранятся в зашифрованном виде
- Callback подписи проверяются перед обработкой
- Используется HTTPS для всех API запросов

## 🐛 Устранение неполадок

### Проблема: "Invalid credentials"

**Решение:**
- Проверьте правильность Merchant ID и Key
- Убедитесь, что вы используете правильные учетные данные для тестового/продакшн окружения

### Проблема: "Currency not supported"

**Решение:**
- Убедитесь, что используете валюту "UZS"
- Проверьте настройки валюты в системе

### Проблема: Callback не работает

**Решение:**
- Убедитесь, что callback URL доступен из интернета
- Проверьте настройки firewall
- Проверьте логи в Integration Request

## 📚 Дополнительная информация

- [Документация Payme](https://developer.payme.uz)
- [Документация Click](https://docs.click.uz)
- [Документация Frappe Payments](https://github.com/frappe/payments)

## 📄 Лицензия

MIT

## 🤝 Поддержка

Для вопросов и проблем создайте issue на GitHub или обратитесь в поддержку.
